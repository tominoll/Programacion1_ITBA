// para compilar en la consola, usar: "make" o "gcc main.c juego.c random.c get_number.c -o life"
#include "juego.h"
#include <stdio.h>

int main(void) 
{ 
    juego_vida(); // juego de la vida de Conway

    return 0;
}
